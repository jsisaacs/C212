package C212.extra;

public abstract class Shape {
    int x, y;



    //go from {} to ; to make it an abstract class, which will still return a type of double,
    //but doesn't require a return statement in the method.
    public abstract double area();
}
